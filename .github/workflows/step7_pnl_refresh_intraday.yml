name: 🔑 Step 7 - pnl_refresh_intraday

on:
  workflow_dispatch: # Allows manual trigger from "Actions" tab

jobs:
  test-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # PATCH: Added python-dotenv to support environment loading
          pip install pandas numpy supabase python-dotenv

      - name: Run Test Sync
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          echo "🧪 Running Isolated P&L Sync Test..."
          python scripts/7_pnl_refresh.py_intraday

